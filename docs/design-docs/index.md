# Design docs index

| Doc | Status | Summary |
| --- | --- | --- |
| [core-beliefs.md](./core-beliefs.md) | ✅ verified | Why the repo is structured this way. |

Add a row here whenever a new design doc is added. `status` is one of:
`✅ verified` (matches current code), `🟡 stale` (needs review), or
`🔴 superseded` (kept for history only, linked from its replacement).

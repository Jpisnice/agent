# Active execution plans

Nothing in flight yet for this minimal seed. When a piece of work is bigger
than a one-off skill/tool addition, add a plan here as
`<short-name>.md` with: goal, current state, steps, decision log. Move it to
`../completed/` when done — don't delete it, so future agents can see the
reasoning trail.
